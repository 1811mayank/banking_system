<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%# <a class="navbar-brand" href="#">Banking System</a> %>
  <%= link_to ("Banking System"), root_path , class:"navbar-brand"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <%= link_to ("Home"), root_path , class:"nav-link"%>
      </li>
      
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle active" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
          <% if user_signed_in? && current_user.admin?%>
            <%= "Admin"%>
          <% elsif user_signed_in?%>
            <%= current_user.first_name.capitalize %>
          <% else %>
              <%= "Welcome"%>
          <% end %>
        </a>
        <div class="dropdown-menu">
          <% if !user_signed_in?%>
            <%= link_to ("sign_in"), new_user_session_path, class:"btn btn-outline-success my-2 my-sm-0" %>
          <% end %>        
        </div>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <% if !user_signed_in?%>
        <%= link_to ("sign_in"), new_user_session_path, class:"btn btn-outline-success my-2 my-sm-0" %>
      <% end %>
      <% if user_signed_in?%>
        <%= link_to ("sign_out"), destroy_user_session_path, method: :delete, class:"btn btn-outline-success my-2 my-sm-0"%> 
      <% end %>
    </form>
    
  </div>
</nav>